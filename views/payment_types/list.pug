extends ../layout

block content
    h1= title
    table
        tr 
            th id
            th Название
        each item in payment_types
            tr
                td= item.id
                td= item.label
